# This configuration file was auto-generated by the Puppet configuration
# management system.  Any changes you make to this file will be overwritten
# the next time Puppet runs.  Please make configuration changes to this
# service in Puppet.

# This uses s3cmd, so make sure you've configured access keys

<% if @when != '' %>when = <%= @when %><% end -%>

# Source folder to back up.  Currently takes a single location.
src = <%= @source %>

# Target bucket (bare name, no protocol handler
bucket = <%= @target %>

# Path to s3cmd
#S3CMD = /usr/bin/s3cmd
S3CMD = <%= @s3cmd %>

# options to pass to s3cmd
options = <%= @options %>
